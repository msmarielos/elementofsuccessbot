meta:
  environment: docker
  toolchain:
    name: docker

build:
  dockerfile: Dockerfile

run:
  containerPort: 3000
  command: ""
  persistenceMount: ""
  
# Переменные окружения добавляются через панель управления Amvera
