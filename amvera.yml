meta:
  environment: docker
  toolchain:
    name: docker

build:
  dockerfile: Dockerfile

run:
  command: ""
  persistenceMount: ""
  
# Переменные окружения добавляются через панель управления Amvera
